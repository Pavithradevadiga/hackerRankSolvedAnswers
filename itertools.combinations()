# Enter your code here. Read input from STDIN. Print output to STDOUT
from itertools import combinations
n = input().split()
lst = sorted(list(n[0]))
for i in range(1,int(n[1])+1):
    m = list(combinations(lst,i))
    o = sorted(m)
    for i in o:
        print(''.join(i))
